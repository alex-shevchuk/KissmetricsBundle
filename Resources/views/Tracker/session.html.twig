<script type="text/javascript">
	var {{ kissmetrics_sessiontracker.kmq }} = {{ kissmetrics_sessiontracker.kmq }} || [];
	function _kms(u){
		setTimeout(function(){
			var s = document.createElement('script');
			var f = document.getElementsByTagName('script')[0];
			s.type = 'text/javascript';
			s.async = true;
			s.src = u;
			f.parentNode.insertBefore(s, f);
		}, 1);
	}
	_kms('//i.kissmetrics.com/i.js');
	_kms('//doug1izaerwt3.cloudfront.net/{{ kissmetrics_sessiontracker.apiKey }}.1.js');

	{% if kissmetrics_sessiontracker.hasQueue() %}
		{% for item in kissmetrics_sessiontracker.getQueue() %}
        {% autoescape false %}
			{{ kissmetrics_sessiontracker.render(item) | }}
        {% endautoescape %}
		{% endfor %}
	{% endif %}
</script>
