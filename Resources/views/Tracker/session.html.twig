<script type="text/javascript">
	var {{ _view.kissmetrics_sessiontracker.kmq }} = {{ _view.kissmetrics_sessiontracker.kmq }} || [];
	function _kms(u){
		setTimeout(function(){
			var s = document.createElement('script');
			var f = document.getElementsByTagName('script')[0];
			s.type = 'text/javascript';
			s.async = true;
			s.src = u;
			f.parentNode.insertBefore(s, f);
		}, 1);
	}
	_kms('//i.kissmetrics.com/i.js');
	_kms('//doug1izaerwt3.cloudfront.net/{{ _view.kissmetrics_sessiontracker.apiKey }}.1.js');

	{% if _view.kissmetrics_sessiontracker.hasQueue() %}
		{% for item in _view.kissmetrics_sessiontracker.getQueue() %}
			{{ _view.kissmetrics_sessiontracker.render(item) | safe }}
		{% endfor %}
	{% endif %}
</script>
